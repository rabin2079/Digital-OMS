import { prisma } from '@/lib/prisma';
export async function POST(req:Request){const f=await req.formData();await prisma.setting.upsert({where:{id:1},update:{companyName:String(f.get('companyName')),whatsappNumber:String(f.get('whatsappNumber')),nepaliYearShort:String(f.get('nepaliYearShort')),nepaliMonthNumber:String(f.get('nepaliMonthNumber'))},create:{id:1,companyName:String(f.get('companyName')),whatsappNumber:String(f.get('whatsappNumber')),nepaliYearShort:String(f.get('nepaliYearShort')),nepaliMonthNumber:String(f.get('nepaliMonthNumber'))}});return Response.redirect(new URL('/settings',req.url));}
