version: "3.9"
services:
  digital-oms:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: digital-oms
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
